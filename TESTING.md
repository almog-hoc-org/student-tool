# בדיקות — "הדרך לדירה"

## בדיקות אוטומטיות (חובה לפני כל merge)

```bash
bun install
bun run typecheck   # tsc --noEmit — אפס שגיאות
bun run test        # vitest — בדיקות יחידה על כל מנועי החישוב
bun run build       # vite build
```

שלושת השלבים רצים גם ב-CI (‎.github/workflows/ci.yml) והם חוסמים.

### מה מכוסה בבדיקות היחידה

| קובץ | מה נבדק |
|---|---|
| `purchase-tax.test.ts` | גבולות מדרגות מס רכישה (מוקפאות 2025-2027), כל סוגי הרוכשים, שוויון תושב חוץ למשקיע |
| `annuity.test.ts` | נוסחת שפיצר: החזר/קרן הפוכות זו לזו, clamp יתרה לאפס |
| `mortgage-calculator.test.ts` | ערכי החזר ידניים, ריבית משוקללת, לוח סילוקין מתאפס, רגישות מונוטונית |
| `budget-calculator.test.ts` | LTV לפי סוג רוכש (75%/50%), תקרת החזר 40% מההכנסה, DTI מול הכנסה |
| `business-plan.test.ts` | עלות עסקה, תזרים, סדר תרחישים, IRR, יתרה בתקופת החזקה ארוכה |
| `side-costs.test.ts` | פריטי עלויות נלוות כולל אגרת טאבו קבועה |
| `storage-sync.test.ts` | סנכרון ענן↔מקומי לפי חותמות זמן, ניקוי בהתנתקות |

הרחבה: כל תיקון באג חישובי חייב להגיע עם בדיקה שמוכיחה אותו.

## צילומי מסך אוטומטיים (בדיקה ויזואלית)

```bash
# טרמינל 1 — שרת פיתוח עם עקיפת auth (פועלת רק ב-DEV, נמחקת מ-production build)
VITE_E2E=1 bun run dev

# טרמינל 2 — צילום כל העמודים בדסקטופ (1280×800) ומובייל (390×844)
bunx tsx scripts/e2e-screenshots.ts
```

הסקריפט מצלם את כל 9 העמודים בשני גדלים, אוסף שגיאות קונסולה, ומחזיר
exit code שגוי אם היו. שגיאות רשת ל-Supabase בסביבה בלי אינטרנט הן צפויות.

## תרחיש אימות מספרי ידני (בדפדפן)

1. **תקציב**: הכנסה ‎20,000‎, הון ‎400,000‎, דירה יחידה, 25 שנה →
   "משכנתא לפי תזרים" חייבת להיות ‎₪1,368,480‎ (תקרת החזר ‎8,000 = 40%‎).
2. **תוכנית עסקית**: מחיר ‎1,200,000‎, דירה נוספת/משקיע →
   שורת מס רכישה חייבת להציג ‎₪96,000‎ (8%).
3. **בדיקת נכס**: בחירת "דירה נוספת/משקיע" חייבת להקפיץ את רצפת
   ההון העצמי ל-50%.
4. **משכנתא**: מסלול פריים יחיד של 100% מהתמהיל חייב להציג אזהרת
   66%; החזר מעל 40% מההכנסה חייב להציג אזהרת DTI.

## צ'קליסט ידני משלים

- RTL: כל העמודים מיושרים לימין; ה-popover של ההתראות נפתח לכיוון הנכון.
- מצב כהה: אין הבזק לבן בטעינה; כרטיסי התרחישים קריאים.
- מובייל: 5 פריטי ניווט תחתונים; "נכסים" מוביל לבדיקת נכס עם טאב
  משני להשוואת עסקאות.
- התנתקות: אחרי logout, משתמש אחר באותו דפדפן לא רואה נתונים קודמים.
